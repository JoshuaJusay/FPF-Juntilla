const Grid = require('./Grid'); // Adjust the path according to your project structure
const Node = require('./Node'); // Adjust the path according to your project structure

const grid = new Grid(10, 10);
console.log('Grid instance:', grid);